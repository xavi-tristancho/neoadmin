{
  "name": "package-based",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "nx run-many --target=dev",
    "test": "nx affected --target=test"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "@nrwl/nx-cloud": "latest",
    "nx": "15.6.3",
    "prettier": "^2.6.2",
    "vitest": "^0.28.5"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
